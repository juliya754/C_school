#include <stdio.h>

sum(a,b)
{
  int c = a + b;
  printf("%d", &c);
}

int main()
{
  
  int a, b, ret;
  printf("Enter a, b: \n");
  ret = scanf("%d %d", &a, &b);
  if (0 != ret)
    printf("%d", ret);
  
  return 0;
}
